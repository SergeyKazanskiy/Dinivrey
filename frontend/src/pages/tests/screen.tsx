import { Container, Text, Button, Spacer, HStack, Box, VStack, SimpleGrid, Image } from "@chakra-ui/react";
import {ContainerWidget} from '../../components/ContainerWidget'


export const TestsPage: React.FC = () => {
    //const { studentInfo } = useStore();
    //const {setStudentInfo, saveStudentInfo } = useStore();

    return (
       <Text color='black'>Tests</Text>
    )
};
